# Independent online review

**Follow-up correction from actual runtime evidence:** The source hypothesis that this active-owner refusal necessarily stops automatic reconnection was contradicted by the duplicate-tab diagnostic below: the installed server/client repeatedly retried after conflict plus transport closure without receiving namespace DISCONNECT. Do not treat the missing-retry scenario as reproduced. The earlier observer failure remains unexplained.

## N3 review — 2026-09-08 02:24–02:29 UTC

Reviewer: `online_preflight`, separate from N3 author `navigation_preflight`. This review concerns the new N3 diff, not a claim of independent review of the reviewer's own N1/N2 implementation. Repository: `sungmpar/ft_transcendence_project`, existing main checkout at `2f11bee69c2d55ed938b53f9750119a890396fb7` with the Goal's uncommitted changes.

Method: read current production/test sources, `git diff`, and author-produced logs. **The reviewer did not execute N3 tests, a N3 browser match, or production edits.** The author separately reproduced the finding below with an actual failing regression. N1/N2 fixture65199 was stopped normally, exit 0, only after the root's service reviewer finished using it; that housekeeping is not N3 verification. The root's other fixture was untouched.

### Finding N3-R1 — rejected recovery ACK can cancel the failure transition (P2)

Initial source trace: `frontend/src/arcade/session-recovery.ts:55` structurally validated an active/waiting ACK, then canceled the five-second owner timer before calling `onResponse`. The new `acceptReady` instance guard rejected a different runner instance later, inside `GameView.vue:99`, `InviteGameView.vue:onRecovery`, or `SpectateView.vue:84`. Those view callbacks did not turn that rejection into a lookup failure: they set the generic recovery status and returned.

Concrete sequence: accept match41 / instanceA / generation1, request recovery, then receive the current requestId and matchId with active ready instanceB / generation99. InstanceB must not replace the active instance, but consuming this ACK also removed the owner's eventual timeout. A still-waiting page could therefore report recovery while never receiving the retry/lobby failure transition.

The reviewer sent this source finding to author and root before any fix. The author added the direct ACK regression and produced `evidence/n3-review-ack-red.log`: **1 FAIL, 27 filtered out**, with the inadmissible response wrongly delivered to the callback. The filtered cases are a focused `--testNamePattern` reproduction, not tests deleted or permanently skipped. Requested correction: apply the same pure ready-admissibility condition before consuming the ACK; inadmissible replies must leave the owner timeout intact. Do not pre-accept/mutate the ready and then reject it as a duplicate when the view consumes it.

Author correction: `canAcceptReady` is now a pure predicate used both by direct ready handling and by the ACK path before canceling the owner timer. The reviewer re-read `session-recovery.ts:27–40,59–62` and the exact ACK regression at `online-session.spec.ts:296`. Inadmissible responses return without consuming the owner request; after five seconds the normal unknown-result callback remains available. The direct ready path remains the only place this predicate commits a new current instance/generation. **Correction accepted on source inspection.** The author's final client test result is recorded separately below; the reviewer did not execute it.

### Other checks and source conclusions

| Concern | Reviewed evidence / conclusion |
| --- | --- |
| Instance and clock identity | `shared/protocol.ts:13` introduces a bounded `instanceId` and nonnegative safe integer `clockEpoch`. `isSnapshot` requires them; `isReadyMessage` delegates snapshot validation; recovery active/waiting responses delegate ready validation. The fields are independent of paddle input generation. |
| Pause and discarded simulation time | `ServerMatchRunner.start:46` advances the clock epoch only on an actual stopped-to-started transition. `advance:122` advances it only when the existing fixed clock reports dropped time. Input replacement does not advance the clock epoch. Core rules, the catch-up cap, and fixed-clock code are unchanged. |
| Full-state and regular packet copies | Runner `snapshot`, GameService `ready`/`setData`, both OnlineSession capture sites, buffer-owned snapshot copying, and the browser unit fixture propagate both required fields. No production copy path dropping a new field was found. Frontend production imports still use the pure shared protocol/core; the new `crypto.randomUUID` import remains in the server runner. |
| Old packets | Buffer receive rejects other same-match instances, lower clock epochs, non-increasing sequence, regressing ticks, and regressing arrival timestamps before re-anchoring. Equal tick/arrival is allowed intentionally for full snapshots at a paused tick; sequence must still increase. OnlineSession separately rejects the wrong match/instance before entering the buffer. |
| Normal jitter and monotone presentation | Within one epoch, the lower-envelope `Math.min(offset, observedOffset)` is preserved. Display clamps local time and target presentation time, retaining the existing no-reversal policy and bounded underflow freeze. An ordinary delayed packet does not reset the buffer. |
| Explicit resync policy | Only a higher epoch discards interpolation history and re-anchors. It explicitly snaps to the first state of that epoch, then builds interpolation from later states. Prior diagnostic counters remain cumulative, including underflows; history reset does not disguise earlier underflows. Existing point/serve/config discontinuity checks still prevent interpolation across reset boundaries. |
| Rendering and input ownership | OnlineSession resets renderer trail/flash state on an accepted epoch change while retaining its keyboard/RAF/socket ownership and original input generation. New snapshots do not create a client simulation or grant observer input. |
| Strict recovery criterion | `clock-recovery.spec.ts` combines the real runner and buffer with 10ms synthetic scheduler samples and 50ms base transit. It requires presentation tick and every moving coordinate to be strictly inside two actual post-resume snapshots with the corresponding lerp values, within the predeclared 300ms bound. This is stronger than movement or a fixed underflow count. |

### Read execution evidence and limitations

The reviewer read `evidence/n3-clock-final.log` and `.json`, whose author-produced result is **8 PASS**. The JSON includes real captured interpolation endpoints, 60Hz simulation / 20Hz delivery, 200/500/1000ms stalls, three repeated stalls, ±40ms synthetic arrival jitter, a deliberate pause/resume, and two separate runner instances. This is synthetic clock/arrival evidence. It is not a browser event-loop impairment, TCP outage, process restart, DB restoration or human-perception test.

After the correction, the reviewer read `evidence/n3-client-final.log`: **28 PASS**, including the exact inadmissible-ACK case. No other confirmed actionable N3 defect was found in this bounded review. This closes N3-R1 for source review and supports continuing to D; it does not replace the root's final matching-bundle integration/browser regression.

The new required snapshot fields make old and new server/frontend bundles incompatible despite retaining the numeric v1 tag. This review accepts no fallback that fabricates a clock identity; final documentation and the final integrated fixture must explicitly use matching updated bundles. D event identity/queue/presentation/audio behavior has not been reviewed in this entry.

## D follow-up

Baseline read before D production edits: the current runner returns step events to GameService, but snapshots do not carry them; OnlineSession draws buffered state without invoking `CourtRenderer.events` or `AudioFeedback.play`. The shared renderer already contains flash/trail behavior, and local audio uses real oscillators after a gesture. These source observations establish the missing online link; they are not an executed D browser test.

Root assigned independent final browser preparation to this reviewer. New owned harness files are `scripts/browser-online-final.py`, `backend/test/online-final-browser.e2e-spec.ts`, and `backend/test/online-final-browser-jest.json`; no D production file is edited by this reviewer. The final wrapper uses the existing isolated fixture and actual guest HTTP redirects, then reads only its own Match rows to verify a six-point result. The frontend can be supplied through `ARCADE_FINAL_FRONTEND_DIST` to avoid changing a bundle in use by another reviewer.

The prepared checks use 1440×900 browser contexts for all screenshots. They exercise a real six-point two-client game and mouse rematch, observer-only transport closure while both players remain in the same live match, observer input count zero, authoritative server event counts, actual native Canvas white-paddle/trail calls, actual native AudioContext/oscillator calls, mute, OS reduced-motion preference, online preset/HUD/input consistency and route resource disposal. Native wrappers delegate to the original browser APIs; they do not fabricate game events, replace audio output, inject authentication, assign scores or alter server state. A test paddle follower reads authoritative positions and sends browser keyboard input to keep the observer/UI scenario alive; it is a test driver, not a new production opponent AI.

Preparation status: Python syntax checked; no final D browser result is claimed until the paired D build and runtime are ready. Full Socket.IO payloads, tokens, credentials and account identifiers are excluded from report serialization; only aggregate event/input/audio counters and score outcomes are recorded. Native audio method observation is not human hearing, controlled WebSocket closure is not a WAN/TCP outage, and automated keyboard actions are not human physical-keyboard play. Final D source review and results will be appended here after implementation.

### D source findings, before the final test run

**D-R1 — gesture refusal did not actually prevent playback on a previously running context.** In the first AudioFeedback revision, `setMuted(false)` assigned `muted=false` before checking `navigator.userActivation.isActive`. If an earlier explicit gesture had already created a running context, mute followed by an out-of-gesture unmute reported `gesture-required` but `play` still accepted the next real event. `restoreMuted(false)` on an existing context had the same mismatch. The reviewer reported the definite API state sequence and explicitly noted that the final UI call-path reachability had not yet been inspected. The author accepted a focused regression and a separate authorized-activation gate; final correction/output review is pending.

**D-R2 — disconnect/pause could leave the last white hit flash permanently visible.** The first OnlineSession integration cleared its event queue and audio on `disconnect` and `sessionStatus:waiting`, but not the renderer's trail/flash history. If the last snapshot was within the five-tick hit-flash window, the buffer then froze at that tick, leaving `flashUntil >= state.tick` true until resync. The reviewer requested the same renderer reset already used at explicit clock resync, plus a regression for both disconnect and opponent-waiting boundaries. The author accepted the finding; final correction/output review is pending.

Correction review update: the reviewer read both targeted author-produced failures (`d-review-gesture-red.log`: 1 FAIL; `d-review-flash-red.log`: 2 FAIL). The author introduced an independent `activated` gate: restoring preferences or refusing a gesture clears authorization and silences existing voices; `play` requires authorization in addition to an unmuted running context. Both disconnect and waiting now clear the renderer as well as pending effects and audio. The reviewer re-read these changes and accepts both fixes. Author-produced follow-up logs read by the reviewer show **14 feedback PASS** and **30 client PASS** in `d-feedback-after-review.log` and `d-client-after-review.log`. These are not tests executed by the reviewer. Final paired browser verification remains pending.

Early positive checks: the bounded event queue consumes only validated, buffer-accepted snapshots; initial ready sets an event-cursor baseline; event IDs/ticks are ordered and bounded; 20Hz snapshots preserve between-broadcast events through non-destructive history; one spectator's full state no longer changes the broadcast scheduling tick; late effects beyond six ticks are skipped; and immediate terminal flush selects only actual same-tick point/finished events from the queue. These are source conclusions while D is still being implemented, not a completed D verdict.

### Independent harness review before execution

The root reviewed the new actual-browser test before its first run and identified a test-strength gap: waiting for a new server hit packet alone can precede its approximately 100ms delayed presentation. An immediately unchanged audio/flash counter could therefore falsely pass mute or reduced-motion checks. **Feedback accepted.** The observer now captures the unique native server hit's tick and clock epoch, then requires the same epoch's presentation tick to reach that event, the presentation/received counters to increase, and the skipped-effects counter to remain unchanged before asserting native mute or motion invariants. No production file was changed for this test correction.

The paired D source freeze was reviewed against `/private/tmp/ft-transcendence-d-dist`, author-reported hash `43e00e78cf2258df`. The independent reviewer re-read the final bounded event-history validator/copies, runner scheduling, presentation cursor/queue/late policy, session reset paths, native-audio activation and voice teardown, Shell gesture handling, online preset storage and active-side keyboard filter. No further confirmed D source defect was found in that bounded inspection. Numeric protocol v1 still requires matching updated server and frontend snapshot fields; compatibility with an old bundle is not claimed. Actual final browser results are recorded below only after their commands finish.

The reviewer subsequently read the author's `d-feedback-expanded.log` (18 PASS across four suites), `d-client-expanded.log` (34 PASS), and `d-online-services.log` (all three existing online suites completed, 15 tests total). These remain author-run evidence.

### First independent paired browser run

Actual command `e-final-online-first` is recorded verbatim with cwd and exit 1 in `evidence/final-online-execution-record.json`. The native browser completed a **6:1 first-to-six result, actual mouse rematch into the same new match on both clients, and route cleanup**. The reviewer opened the 1440×900-viewport full-page screenshot `final-online-first/final-online-six-point-result.png`; scores, stored-result notice, rematch action and controls were visible without overlap.

The aggregate run failed at a transient `복구` status-text wait in the observer case. It did not reach that case's stable live-tick/input assertions or any effects case, and its post-browser DB assertion was not reached. This is preserved as FAIL, not an online completion claim. Root source review pointed out that ready/session status may immediately replace the recovery message. The harness now requires a new actual WebSocket, greater spectator session generation, the same owned watched match, and a tick newer than the pre-disconnect tick; it still requires subsequent tick advancement, zero observer input, unchanged player connections, and both players remaining live before and after screenshots. This strengthens state evidence while removing an unstable text-only precondition. Failure diagnostics record only aggregate connection/tick/phase/input values and status categories, without credentials or account identifiers.

The independent observer-only retry actually **passed** (`e-final-online-observer`, exit 0): new native WebSocket plus greater spectator generation; same owned match and advancing tick; zero observer application and native transport inputs; both players' connection counts unchanged; both remained in that live match before and after two captures. No uncaught page error was observed. Python took 64.403s; Jest took 74.842s. The disposable fixture had no completed Match row afterward, consistent with this live-rally case; this is not a six-point persistence check.

The reviewer opened both `final-online-observer/final-observer-reconnected.png` and `final-online-observer/final-observer-peer-still-playing.png` at their original 1440×900 viewport/full-page capture. Both show the same 2:2 live court, separate player/observer labels and controls, a recovered-state notice on the observer, and no end overlay. The captures occur at different instants; their ball positions are not a simultaneous visual-sync measurement. All own contexts and the fixture closed normally before the root's service reviewer used Chrome.

A second independent agent (`live_gate`) read the final harness without executing or editing it and found no confirmed false-positive blocker. They checked native-method delegation, real-keyboard follower scope, stable observer ownership/tick assertions, delayed hit presentation before mute/motion checks, and cleanup. One precision clarification was retained: the first observer PASS checked observer input zero before screenshots and player live ownership again afterward. The final all-case run additionally rechecks observer application/native input zero after screenshots. Synthetic automatic play and native-audio observation limits remain explicit.

### Actual effects attempts and retained limits

`e-final-online-effects` exited 1, while its JSON records five reached feedback checks: default muted with an actual server hit and native Canvas white flash, native AudioContext/oscillator creation after the explicit sound gesture, mute after a presented hit, and both OS/explicit reduced-motion suppression after presented hits. The subsequent WASD court click was intercepted by a normal match-complete overlay. The reviewer opened its reduced-motion PNG (live 0:3 capture, no trail); keys and route disposal were not reached in that attempt.

The test follower was corrected to release keys while a settings control owns focus and to repeat the actual movement key periodically while the court owns focus; it still never changes application/server state. The unchanged WASD/HUD/native-input assertions now run immediately after a fresh match starts. Two redundant final court clicks were removed from the later sound/settings/menu-only portion; the sound button itself must produce native audio readiness, and all actual hit/presentation/mute/motion/disposal assertions remain.

`e-final-online-effects-retry` also exited 1. It reached actual WASD/HUD/outgoing movement and inactive-arrow checks plus default-muted and native-audio-unlock checks. The original strict guard then found a changed cumulative skipped-event count during the mute observation, so no mute/motion success was claimed for that run. This guard also included older queued point/serve/late boundaries that could precede the candidate hit. The next harness revision keeps one total 45-second bound and records every discarded observation window: after a skip/epoch boundary it requires a **new unique hit** in a new window, same owner and epoch, hit tick reached by presentation, increased received/presented counters and zero additional skips. Native audio/flash/trail invariants remain anchored to the original caller baseline across all windows. No cumulative counter is reset and no audio/flash violation is excused.

### Independent D browser pass and source verdict

The independent selected effects run `e-final-online-effects-stable` actually **passed**, exit 0, Python 63.771s / Jest 76.202s. It confirms the WASD HUD and actual outgoing direction, inactive arrows under WASD, default mute with real authoritative hit/native Canvas flash, explicit gesture creating a real running AudioContext and oscillator, mute across later displayed hits, OS and manual reduced-motion suppression, native audio context closure/zero active oscillators on route exit, saved online settings on back navigation, and no automatic context creation from the restored unmuted preference. No uncaught page error occurred.

The retained JSON includes exact successful presentation windows: mute server hit tick 1772 at presentation 1772.81; OS motion tick 1968 at presentation 1968.80; explicit motion tick 2056 at presentation 2056.822. Each successful window had zero additional skips. Mute re-armed after two prior boundaries with six skipped events each; this is explicitly recorded, and its no-new-tone invariant spans those boundaries from the original mute baseline. The native audit observed one AudioContext, one oscillator start, 15 unique server paddle events and 17 white-paddle drawing calls. Drawing calls are frames, not unique hit counts; they are not compared as an event dedup count.

The reviewer opened both 1440×900-viewport/full-page images. `final-online-effects-stable/final-online-reduced-motion.png` shows a live 1:5 court, no visible ball trail, the W/S/D HUD and explicit reduced setting. `final-online-effects-and-key-hint.png` shows the subsequent natural 1:6 result with sound enabled and W/S/D still displayed; it is **not a screenshot of the instant of a hit effect**. Actual effect timing is established by native Canvas/audio observation and the presentation-window records, not inferred from that static image. This fixture's actual Match query also returned one 6:1 result, but the effects-only case does not enforce a minimum six-point result: the root's full browser group will execute the all-case wrapper and its formal match persistence assertion.

The root requested that this reviewer hold a redundant standalone all-case rerun after the selected observer/effects passes. The new three-case wrapper/config and Python harness are now handed over for the root's complete browser command. All reviewer-owned Chrome contexts and fixtures closed normally. The reviewer changed no D production code. N3-R1, D-R1 and D-R2 are accepted/fixed on source and corresponding author red/green evidence; no remaining confirmed source blocker was found in the bounded N3/D review. Browser/runtime limits remain real headless loopback execution, controlled WebSocket closure and native method observation: no WAN, physical-human play or human-hearing result is claimed.

New wrapper lint initially could not run under the existing backend project parser because that tsconfig excludes all test files. A command-local `project:null` parser override applied the same existing recommended/Prettier rules and found 22 formatting errors; the existing formatter fixed only this new wrapper. The final scoped command passed, exit 0. This is distinct from a full repository lint pass, and no lint configuration, dependency, test assertion or production source was changed. Exact commands and exits are in `evidence/final-online-execution-record.json`.

### Retained browser key-timing limit

After handoff, the reviewer found a limited strength concern in the actual-browser inactive-arrow check: it records `inputPackets`, dispatches ArrowDown under WASD, and waits for one later neutral packet. A heartbeat sent between reading the baseline and dispatching that key could satisfy this first-packet condition. The browser result therefore establishes the observed neutral packet after the test action, **not an exact causal timestamp or a sustained held-arrow proof**. The reviewer reported the concern without changing a running/frozen test. The root requested retaining the current run and documenting the limit rather than launching an additional Chrome run.

Independent supporting evidence is narrower and explicit: `online-session.spec.ts:92` dispatches a disallowed KeyW in arrows mode and ArrowDown in WASD mode, requiring `defaultPrevented=false`; it also verifies a neutral packet on layout change and valid KeyS/KeyD movement/action after switching. `online-preferences.spec.ts:16` checks the exact arrows+Space and W/S/D presets and separation from local ArrowLeft Power. Production `KeyboardController.keydown` considers only `activeSide` bindings before accepting/preventing a key; `OnlineSession.useKeyLayout` sends neutral input, clears held keys and selects that side. The author-run client/feedback suites covering these tests passed as recorded above. This source/unit evidence supports the selected-key contract while the native-browser timing limitation remains visible. No production failure has been demonstrated by this concern.

### Root's first full browser group: retained aggregate failure

The reviewer read `evidence/final-browser-command.log` after the root reported exit 1. Its first four configs (13 tests) passed, including the existing actual 6:0 match/rematch/menu and active-player reconnect/paused-tick/active-tab/forfeit checks. The new final all-case wrapper failed after 154.717s. Its JSON records a successful separate 0:6 six-point match/mouse rematch/menu case, then a failed observer case; effects and the wrapper's formal DB assertion were not reached. This is **not a full browser-group pass**.

Unlike the first transient-text failure, this diagnostic shows a real missing live observer connection: both players remained in the same rally at ticks 3629/3632 with one open connection each, while the observer remained at old generation 5 / tick 1618 and had two created WebSockets but zero open ones. Input remained zero. The log does not record the rejection/close reason, so it cannot establish whether this was a server active-session refusal, transport failure or another connection path. A close-handshake/offline/reconnect race is a hypothesis, not a dismissal as a test-only issue. The server must preserve active-tab ownership; if a legitimate returning owner can encounter a refusal, its retry/exit feedback is also a product boundary.

After a refreshed gate, the root authorized a diagnostic-only harness update and one later targeted all-case run after their Home/local checks. The harness now records a bounded 64-entry native transport history (ordinal connection, open/error/close code and wasClean), namespace server-disconnect, and allowlisted conflict/authentication/expired/transport/timeout/unclassified categories. It does not output raw frames, raw error messages, tokens or account identifiers. Production retry policy and server authorization are unchanged. The key timing concern above was also strengthened: the native Canvas keydown sequence and held key must be attached by the observer to the actual outgoing packet used for the assertion. Earlier runs retain their stated weaker timing scope; no later pass is claimed until the diagnostic command finishes.

A bounded follow-up read by `navigation_preflight` confirms the installed Socket.IO 4.5.1 behavior: `socket.onDisconnect` for a server namespace DISCONNECT calls `destroy()`, removing manager subscriptions and stopping automatic reconnect; explicit `connect()` re-subscribes/opens. The service's existing active-account check emits its conflict error and disconnects a newcomer when an older session remains registered. Old session deletion occurs through the actual disconnect path before `dropUser`'s first await, so an arbitrary async cleanup lag is not established. The unresolved hypothesis is transport/server recognition ordering or another refusal path, pending native diagnostic output.

All three route views currently call `socket.connect()` only on mount. Their ordinary disconnect handling cancels the state query and says it is waiting for reconnect; the existing match-state retry buttons are disabled when disconnected. Spectate has only menu exit available in that state. After a failed lookup, its retained desiredRoom alone is also insufficient for the current onConnect condition once active/waiting are false. A potential explicit transport retry would need a bounded user-visible attempt plus retained target intent and the existing authenticated full-ready query, while preserving active-tab refusal. This is a source-level retry/feedback gap; the cause of the retained actual failure is not asserted before diagnostic evidence. No production change has yet been made for this follow-up.

### Diagnostic all-case pass — separate from the retained failed command

The reviewer actually executed `e-final-online-diagnostic-all` against the root's latest default dist `4de1388331743be3`: **exit 0**, Python 164.491s / Jest 175.014s. All three cases passed in this command. The full-match case ended 0:6, both clients used the mouse rematch into the same new match, and menu cleanup passed. The observer obtained a new connection and generation in the same live match; both player connections remained unchanged/live, and observer application/native input remained zero before and after screenshots. The effects case includes the strengthened native keydown-sequence-to-outgoing-packet check and all previously described mute/motion/audio/disposal/preferences conditions. The formal post-browser database assertion was reached and passed: four completed fixture Match rows, one actual six-point result, score 6:0. No page errors were observed, and all contexts/fixture closed normally.

The reviewer opened all five captures from `final-online-diagnostic-all`: full 0:6 result, observer/player live 5:3 courts, explicit reduced-motion live 1:1 court with W/S/D HUD, and a later live 1:1 court with sound enabled and the normal ball trail. These use the same 1440×900 viewport as the earlier online comparison; full-page capture heights vary with content. They are separate instants, not simultaneous visual latency measurements.

The successful observer trace records close requested at 30120ms, a clean native close (1005) at 37025ms, offline command completion at 37689ms, a second connection attempt while offline ending with error/unclean 1006 at 38392ms, online command completion at 39236ms, and a third connection opening at 39568ms. The source is browser performance time and native browser events; server-recognition timing is not measured. There is no active-session conflict or namespace server-disconnect in this successful trace. **It cannot determine the cause of the earlier failed root command.** That failure remains UNRESOLVED in the execution history.

At that stage, an unverified source hypothesis suggested that an active-owner refusal could arrive as a namespace server-disconnect and stop automatic recovery. The root authorized an independent actual duplicate-tab regression before a production fix. Its proposed sequence used idle same-auth game tabs, an assumed conflict/namespace-disconnect, original tab closure, read-only aggregate service session count reaching zero, then an observation of automatic or explicit reconnection. The later native evidence below contradicted that assumed wire path. None of this attributes the earlier observer failure to that hypothesis.

### Duplicate-tab diagnostic contradicts the assumed failure path

The first actual idle duplicate-tab probe, `e-rejected-session-before`, exited 1 at its assumed conflict-plus-namespace-DISCONNECT wait (47.553s). It did **not** reach original-tab closure or reproduce the proposed missing retry. The retained second command, `e-rejected-session-before-diagnostic`, also exited 1 at that assumption (45.979s), but captured both page histories and real service ownership before cleanup.

The original page remained at `/game`, with one open socket and enabled play; the fixture service had exactly one registered session. The duplicate page received repeated allowlisted `active-session-conflict` errors followed by clean native transport closes (1005), then automatically created further connections: 19 by roughly 23 seconds. No namespace server-DISCONNECT was observed. Its screenshot, opened by the reviewer, showed the generic disconnected/waiting notice and disabled play button, while the owner remained protected. Thus **the proposed no-automatic-reconnect path is not reproduced and its premise is contradicted in this actual refusal run**.

Installed server source `socket.disconnect(true)` calls `Client._disconnect()`, which asks its sockets to emit namespace DISCONNECT before closing the underlying connection. Code intent alone does not establish that this packet is delivered before the transport closes. The client's namespace-DISCONNECT handler really does destroy subscriptions, but the actual observed path here is transport closure plus automatic retry. This correction was sent immediately to root and the separate source reviewer, and no production change was authorized on the disproven premise. The next bounded observation must confirm refusal using the actual conflict and transport history, then close the original and observe whether automatic recovery occurs. Repeated conflict churn and the overwritten reason are separate potential product concerns, not proof of the earlier observer failure.

### Separate conflict churn regression — reproduced, with automatic recovery retained

The corrected actual command `e-rejected-session-churn-before` exited 1 with **REPRODUCED** (Python 23.182s / Jest 44.401s). It waited for real active-session-conflict plus transport closure, without requiring an unobserved namespace packet. During the next bounded six seconds it observed **five additional automatic connections** and no visible conflict reason. The original remained connected with play enabled and exactly one server session. After closing that original tab, the read-only service observation confirmed the original generation disappeared and count reached zero. The rejected page then **automatically recovered on the same document**, without reload or a manual reconnect button, to one authorized server session. No browser page error occurred; all owned contexts and the fixture were closed.

The reviewer opened both 1440×900-viewport captures: the protected-owner period shows only a generic disconnected/waiting message and disabled play; the post-owner-close image shows a connected lobby with enabled play. This is an expected failing regression for repeated exact-conflict connection attempts and lost explanation. It is a positive counterexample to the earlier hypothesized permanent automatic-stop behavior. The earlier root observer failure remains UNRESOLVED and is not attributed to this newly reproduced issue.

The root separately authorized a minimal client change only for the exact active-session-conflict message: stop that socket's repeated retries, retain the conflict explanation and offer a bounded explicit reconnect action. General transport recovery and initial mount behavior must remain unchanged, and the server's active-owner protection must remain intact. The next independent after check will require no churn while the owner is present, a retained reason, real owner cleanup, and same-page native-button recovery on all three online routes. No source edit was made by this reviewer.

### Narrow conflict-policy patch: independent source review and final harness handoff

The reviewer read the author's new `connection-retry.ts`, its eight tests in `backend/src/game/connection-retry.spec.ts`, and the three route integrations without editing production. The guard matches only the existing exact server conflict message; unrelated errors and first mount leave Socket.IO behavior alone. It publishes the reason before the potentially synchronous own-socket disconnect, coalesces repeated rejection, owns one explicit five-second attempt, invalidates late timers by generation and stops late connections after timeout/refusal. The three routes retain their previous ready/state-query paths and dispose this helper on exit. Spectate carries its retained desired match into the explicit retry's recovery query. No confirmed defect was found in this bounded source review.

The reviewer read author-produced `e-connection-retry-unit-verified.log` (29 PASS, including eight helper tests and the existing 21 navigation tests), `e-connection-retry-lifecycle.log` (10 PASS), and an empty typecheck output whose exit was author-reported. These are not commands independently executed by this reviewer. This test grouping does not verify a physical network or recover the unknown cause of the earlier observer failure.

The reviewer froze the after harness for root execution with the newly paired frontend, root-reported hash `99110685ef45f988`. Its default runs `/game`, `/invite`, and `/spectate` in separate actual guest contexts. Each requires native conflict/close, six seconds with no additional automatic connection attempts, a visible retained reason, the original owner still protected, actual removal of the old server generation, a further 0.75-second stopped interval before the user action, then exactly one native connection from the real reconnect-button click on the same URL/document. It rechecks server ownership, enabled lobby action and zero idle inputs after captures, then closes the context and requires server session count zero before the next route. This idle regression verifies transport/lobby recovery; it does not claim an active-match recovery on all three routes.

Six route-specific captures are requested at the common 1440×900 viewport. The child/Jest deadlines were enlarged for three sequential cases (210/240 seconds), retaining individual waits and all original assertions. Python syntax compilation, same-rule scoped wrapper lint and `git diff --check` actually passed. The root requested one complete six-config browser group, with this regression first, instead of a duplicate standalone after run. No actual after result is claimed before that command completes.

### Actual three-route conflict-policy after result

The root's new full browser group first executed this reviewer-owned regression against the paired `99110685ef45f988` frontend. The reviewer read its completed `final-browser-after-connection-retry/online-rejected-session.json`: **all three route cases PASS**, Python 70.844s, no page errors. Each recorded zero extra native connection attempts in six seconds, visible conflict reason, original owner protected, old generation removed/server count zero after owner close, no automatic attempt in the recorded 0.75-second pre-click interval, then exactly one native connection from the actual reconnect click on the same document. Server ownership, enabled lobby action and zero idle inputs were rechecked after screenshots.

The reviewer opened all six route-specific before/after-reconnect captures. Conflict notices and retry buttons are visible, and the recovered game/invite/spectate lobbies have their normal enabled actions. No overlap was observed at the common viewport. The conflict explanation appears in the existing shell overlay, status and new persistent notice, which is visually redundant but readable; this is a nonblocking observation, not a reason to modify the frozen final build. These idle-route results do not establish active-game recovery for each route. The complete six-config browser command was still running at this review point, so its aggregate exit is recorded only after completion below.

The same root group subsequently logged a failure in the existing lifecycle config: `online-lifecycle-browser.e2e-spec.ts:45` timed out after 30 seconds waiting for the peer's paused notice immediately after `setOffline(true)` (Jest 59.945s). Its later paused-tick, generation recovery, active-tab and forfeit assertions were not reached. The reviewer read the failure and source but did not execute or modify this test. The test has no failure-time report because it assigns its report only at the successful end. Native socket close/server recognition timing and possible natural match completion are therefore unmeasured in this failure; neither a product cause nor a test-only cause is asserted. The final all-case config was not reached in this fail-fast attempt. The successful new three-route regression remains a separate result, not an aggregate group pass.

### Explicit lifecycle fault, preserving the original failure

After the root confirmed the group exit 1, it authorized editing only the existing lifecycle test. The reviewer verified the installed Playwright 1.55 beta's `routeWebSocket`, `connectToServer`, automatic message forwarding and close behavior in installed primary source. The new impairment calls the existing browser WebSocket's close method and briefly rejects only newly attempted connections through Playwright routing, then releases that gate after the real peer has reported pause and held the same tick for 200ms. Other routed traffic connects to the actual Socket.IO server without packet changes. Playwright installs its own WebSocket wrapper, so this is described as a **controlled routed transport impairment**, not an untouched native handshake or WAN test. No application state, input, snapshot or result is injected.

All original pause/tick/generation/side/match/active-owner/forfeit assertions remain. The test now always records safe browser phase/tick/identity categories, bounded transport/fault history and reached checkpoints before cleanup. Its original fixture-created authenticated users remain; this specific P4 case does not claim actual guest-login UI coverage, which is tested separately. Root authorized a P4 command followed by the final three-case config, without repeating the already successful first four configs on the same frozen `99110685ef45f988` build.

The first controlled command `e-final-lifecycle-controlled` exited 1 (74.353s), after the original pause/resume assertions and screenshot had been reached. Only a newly added stronger duplicate-rejection frame check timed out. The diagnostic shows an actual conflict notice and zero open sockets on the rejected page. The added observer incorrectly parsed a Socket.IO frame by slicing two characters, which fails for namespaced `42/game,[...]` frames. This is a confirmed harness observer defect; the parser was corrected to the first JSON array delimiter, retaining the stronger assertion. The earlier setOffline-only failure is still unclassified and is not retroactively attributed to this separate parser error. The reviewer opened the partial-run screenshot showing a live 0:1 recovered court. The retry is recorded only after completion.

The corrected observer command `e-final-lifecycle-controlled-parser-fixed` actually **passed**, exit 0 / Jest 59.053s. The peer remained at tick 317 over the 200ms pause check, then the returning client acquired a greater generation with the same match and side and the peer advanced again. The duplicate emitted a real namespaced active-session-conflict packet, closed, and did not replace the original; menu exit explicitly ended the peer. No page error occurred, and all owned contexts/fixture closed normally. The reviewer opened the 1440×900-viewport screenshot showing a live 0:2 recovered court.

The recorded gate interval was 611ms; no new attempt occurred during that interval, so this run does **not** claim execution of the route's blocked-new-attempt branch. The actual recorded browser close was clean 1000 at 19834ms and the returning connection opened at 21506ms. The positive result verifies the explicit-close pause/reconnect scenario with real server authority and the stated routing mediation. It does not turn the earlier setOffline-only timeout into a pass or determine its cause. The final all-case command on the same frozen source is now running separately.

### Final independent online verification on the frozen current build

`e-final-online-after-conflict` actually **passed**, exit 0 / Jest 170.514s / Python 159.829s, against the unchanged paired frontend `99110685ef45f988`. All three final cases passed: an actual visible 0:6 finish followed by mouse rematch into the same new match and menu cleanup; spectator-only new connection/generation with the same live match and advancing ticks, unchanged player connections and zero observer application/native inputs before and after captures; and authoritative-hit Canvas feedback, native AudioContext/oscillator activation, mute, OS/manual reduced motion, causal native keydown-to-outgoing-input verification, audio disposal and restored preferences without automatic audio creation. The formal actual fixture database assertion was reached: **four completed Match rows and one six-point result, 6:0**. No page error occurred. All owned contexts and the isolated fixture were closed normally.

The reviewer opened every final capture: a 0:6 result; spectator/player live 3:2 courts; and reduced-motion then normal-motion/audio-enabled live 2:1 courts. They use the common 1440×900 viewport. The observer image visibly contains a white paddle hit flash, while the explicit-reduced image has no trail. These are separate instants; exact timing remains supported by the actual presentation/native counters. No overlap was observed.

Successful mute presentation was server tick 611 at presentation 612.886; system motion was 972 at 972.868 after one prior boundary with one skipped event; manual motion was 1431 at 1436.854 after two prior boundaries with three and two skips. Each successful window had zero new skips. The no-new-tone/flash/trail baselines remained anchored before the prior boundaries, so those boundaries do not excuse feedback violations. Native audit recorded one AudioContext, one oscillator start, twelve unique server paddle events and eleven white-paddle drawing calls; drawing calls are frames, not an event count. The observer trace again shows a controlled clean close, one offline connection error and a subsequent third connection opening. It does not resolve the earlier unexplained observer failure.

`navigation_preflight` independently read the final P4 fault/assertion/diagnostic code and found no confirmed false-positive issue. It verified actual close, peer pause, 200ms fixed tick, greater generation with same side/match, advancing peer, actual duplicate conflict, owner retention and menu forfeit. This was read-only review, not an additional test execution. Its requested limits are retained: the rejected-new-attempt branch was not exercised, routing mediates the transport, and the original setOffline-only failure remains preserved.

Final verdict: the bounded source review has no remaining confirmed N3/D/conflict-policy blocker. Current-source online scenarios have successful evidence across the root's first four config results and the separate corrected P4/final-all commands. **Neither failed aggregate browser command is renamed or reported as a complete group pass.** Earlier unknown observer/setOffline failures remain visible as execution uncertainty. The final result covers real headless loopback clients and isolated fixture persistence; WAN behavior, physical human play, human hearing and deployment are not claimed. The duplicated conflict explanation is a readable, nonblocking presentation issue. Exact executor commands and exits are in `evidence/final-online-execution-record.json`. Source/tests and this review are frozen for the root's final packet.
